collections
pip install numpy
upload and install numpy
import data from Python library
set workload to subserver
force library extension install
set collections to LLM
enable lambda
RuntimeWarning classmethod
